# devices

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build devices` to build the library.

## Running unit tests

Run `nx test devices` to execute the unit tests via [Jest](https://jestjs.io).
